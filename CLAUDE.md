# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## プロジェクトの現状

**まだコードは存在しない設計フェーズのプロジェクト**。リポジトリには設計ドキュメント（`Docs/`）とサンプル DBML（`SampleDBML/`）のみがある。実装はこれから[実装計画.md](Docs/実装計画.md)のフェーズ 0 から開始する。

JPDBMLEditor は DBML（Database Markup Language）のビューア/エディタ。既存 VSCode 拡張の「ER 図プレビューの見た目が悪い」「VSCode の器が重い」という不満を解消するため、独立したデスクトップアプリとして自作する。VSCode とは疎結合で、VSCode はテキスト編集に使い続け、本アプリは編集を明示保存（保存ボタン/Cmd+S）し、外部での変更は手動リロードボタンで取り込む（「見る・整える・軽く編集する」を快適にするのが目的。2026-07-14 に即上書き＋ファイル監視から明示保存＋手動リロードへ移行。詳細は [計画-保存UNDO計画.md](Docs/計画-保存UNDO計画.md)）。

## 技術スタック（確定済み）

- **器**: Tauri + Vite + TypeScript（Electron より軽量。前例プロジェクト AnimeForgeStudio の構成を参考にする）
- **UI コンポーネント**: MUI（トーストは notistack）
- **DBML パーサ**: `@dbml/core`（DBML 公式 TS パーサ）
- **ER 図描画**: React Flow + 自動レイアウト（ELK.js に確定。dagre は比較の末削除済み）
- **保存/取り込み**: 編集はメモリ上に保持し、明示保存（保存ボタン/Cmd+S）でのみファイルへ書き込む。ファイル監視（fs watch）は廃止し、外部（VSCode 等）での変更は手動リロードボタンで取り込む（[計画-保存UNDO計画.md](Docs/計画-保存UNDO計画.md)）
- **メタデータ**: `.dbml` の隣に `foo.jpdbml.json` を併置（配置座標などの見た目状態。DBML 本体は汚さない）

## アーキテクチャの要点

実装計画（[実装計画.md](Docs/実装計画.md)）が想定するディレクトリ構成:

```
src/                 # フロント（Vite + TS + React）
├── parser/          # @dbml/core ラッパ・内部モデル変換
├── graph/           # 隣接グラフ・N ホップ探索
├── layout/          # dagre/ELK レイアウト
├── view/            # React Flow ノード/エッジ・検索UI
├── edit/            # 最小編集ロジック
└── meta/            # .jpdbml.json 読み書き
src-tauri/           # Tauri（Rust: ファイルI/O・監視）
```

データフロー: `.dbml` → 開く/手動リロードで読み込み → `@dbml/core` でパース → 検索/フォーカス状態でフィルタ → 自動レイアウト → React Flow で描画 →（編集時）最小編集でメモリ上のテキストを更新（Undo履歴に積む）→ 明示保存。

### 横断的に効く 3 つの設計原則（複数モジュールに影響するため要注意）

1. **編集は「最小編集で書式保持」**（`edit/` の中核方針）。全体を逆生成せず、変更箇所だけをテキストに最小編集し、コメント・並び順・整形を壊さない。MVP はカラム追加（末尾に 1 行差し込み）から。**ただし将来のコピペ・行並べ替えを見据え、「末尾固定」ではなく「カラム単位の位置」を扱える構造で最初から作る**（後付けで大改修にしない）。

2. **表示は 3 モードの排他状態**（`view/` と状態管理の中核）。`全体` / `絞り込み`（検索入力時）/ `フォーカス`（テーブルクリック時、N ホップ近傍表示）。現在モードは常に画面右上に明示。モード遷移の詳細は [UI設計.md](Docs/UI設計.md#表示モードと状態遷移)。

3. **`.jpdbml.json` は「あれば使う」補助情報**。無い/壊れている場合は自動レイアウトにフォールバックする。`.dbml` 本体には見た目状態を書き込まない。

### 最大のリスクと spike

最大の技術リスクは「最小編集で書式保持」を支える `@dbml/core` の位置情報。パース結果から元テキスト上の**テーブル/カラム単位のレンジ**が取れるかを、フェーズ 1 の中で小さく確認（spike）してから本格実装（フェーズ 4）に入る。取れなければ自前の軽量位置マッピングを実装する。この結果が `parser/` と `edit/` の実装方式を左右する。

## 実装の進め方

「見る→絞る→フォーカス→編集」の順に層を積み上げる。**各フェーズ完了時に必ず単体で動くもの**ができる状態を保つ（手戻り最小化）。詳細な完了条件は[実装計画.md](Docs/実装計画.md)を参照。

- フェーズ 0: Tauri + Vite + TS スキャフォールド、依存導入、HMR 確認
- フェーズ 1: 読み込み + 全体 ER 図 + ファイル監視（+ 位置情報 spike）※ファイル監視は後にフェーズ6で廃止し手動リロードへ移行
- フェーズ 2: 検索フィルタ（テーブル名・カラム名、カラムヒットはハイライト）
- フェーズ 3: 近傍フォーカス（N ホップ、UI 可変）
- フェーズ 4: インラインカラム追加 + 最小編集保存 + 外部変更検知ダイアログ ※保存衝突ダイアログは後にフェーズ6で廃止
- フェーズ 5: 配置座標を `.jpdbml.json` に永続化
- フェーズ 6: 明示保存 + 無限 Undo（ファイル監視廃止・手動リロード化）。詳細は [計画-保存UNDO計画.md](Docs/計画-保存UNDO計画.md)

## サンプルデータ

`SampleDBML/` には動作確認用の実データ DBML をローカルに置いている（`.gitignore` 済みでリポジトリには含まれない）。大規模なもの（約 1300 行、42 テーブル、Ref・Enum 多数）と小規模なもの（約 360 行、12 テーブル、Ref なし）があり、`Enum` 定義・大量の日本語 `note`・コメントブロックを含むため、パーサ検証や描画/検索の動作確認、および「書式保持」が壊れていないかの確認に使える。

## 規約

- 設計ドキュメント（`Docs/`）はすべて日本語。ドキュメント同士は相対リンクで相互参照している（例: `[全体設計.md](Docs/全体設計.md)`）。新規ドキュメント追加時もこの参照スタイルに揃える。
- コミットは各フェーズ完了時に行うが、**ユーザーから指示があったときのみ**実施する。

### テキスト入力の IME 対応（必須）

日本語ユーザー向けアプリのため、**すべてのテキスト入力（`TextField`/`input`/`contentEditable` 等）で IME（日本語変換）を必ず考慮する**。新規のテキストフィールドを追加したら、以下を漏れなく実装すること。

- **Enter/Esc の keydown で確定・破棄・ショートカット発火を行う箇所**は、IME 変換確定/キャンセルの Enter/Esc を誤発火しないようガードする。
  - `e.nativeEvent.isComposing`（グローバル `KeyboardEvent` なら `e.isComposing`）に加え、**`keyCode === 229`（IME 処理中）を必ず併用する**。`isComposing` 単独では、環境（OS/ブラウザ/IME）により変換確定 Enter の keydown で既に `false` になっており取りこぼすため。
  - 例: `const isComposing = e.nativeEvent.isComposing || e.nativeEvent.keyCode === 229;`
- **入力中に onChange で親へ通知・検索・副作用を走らせる箇所**は、`compositionstart`/`compositionend` で変換中フラグを持ち、**変換中は通知せず `compositionend` で確定値を通知する**（未確定文字列で処理を走らせない）。実装例は [SearchField.tsx](src/view/SearchField.tsx)、[ColumnEditRow.tsx](src/view/ColumnEditRow.tsx)。
